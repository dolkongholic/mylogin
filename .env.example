# mangomail과 동일한 Postgres(Supabase)에 연결해 동기화합니다.
# mangomail/.env.local 의 DATABASE_URL 값을 그대로 복사하세요.
# (앱 내 '서버 동기화 → 연결 저장'으로 입력해도 됩니다)
DATABASE_URL=

# (선택) mangomail의 NEXTAUTH_SECRET — 현재 금고는 제로지식이라 필수 아님
NEXTAUTH_SECRET=
